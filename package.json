{
  "name": "launchpad-react-template",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "concurrently \"npm run start --prefix backend\" \"npm run dev --prefix frontend\"",
    "build": "npm run build --prefix frontend",
    "test": "echo \"No tests specified\" && exit 0"
  },
  "dependencies": {
    "concurrently": "^6.0.0"
  },
  "devDependencies": {}
}